extends layout.pug

block content
  h2 Выберите репозиторий  
  .repos
    ul.repos__list
      each repo in repos
        li.repos__item
          a.repos__link(href= `/branches/${repo.name}`)= repo.name
  

  